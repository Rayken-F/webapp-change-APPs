# IQC Hybrid Image RC v19 — Field Note (2026-08-24)

## Scope
RC v19 only stabilizes the Hybrid AI sync button/visual state and preserves the v18 result-card stability fix. It does not promote the IQC Hybrid flow to Production.

## Field result
- Login banner correctly advances to RC v19 after removing the legacy v18 `markVersion()` overwrite from `iqc-hybrid-stuck-guard-v15.js`.
- Hybrid AI sync button no longer flashes between enabled/disabled visual states.
- User confirmed: **no flicker**.
- Result: **UI stability PASS**.

## Current production status
- RC only.
- Formal DS Workstation / Production remains unchanged.
- Cloud OCR end-to-end, receipt idempotency, and IQC write safety are still pending field verification.

## Next acceptance gate
1. Use a batch that enters `AI_PENDING`.
2. With `Cloud 已就緒`, tap `同步 AI 待辨識` once.
3. Confirm one pending photo becomes `AI_VERIFIED` / `AI已補完`.
4. Confirm `IQC_AI_Receipt_RC` gets exactly one receipt row for the fixed `job_id` and stores OCR text/digest only, not Base64/image bytes.
5. Retry the same fixed job and confirm no second Cloud Vision call / duplicate receipt is created.
6. Only after these checks proceed to Hybrid merge/business-rule validation.
