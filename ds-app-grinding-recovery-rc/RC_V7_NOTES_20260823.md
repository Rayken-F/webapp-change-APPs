# DS Grinding Recovery / IQC Image RC v7 — 2026-08-23

## Scope
RC-only. Production `ds-app/` and formal IQC write path remain unchanged.

## IQC Honeywell image recognition v2
- Preload Tesseract.js on RC entry.
- Install `iqc-ocr-multipass-hook-rc-v2.js` before the existing image intake module.
- First pass remains local OCR.
- If raw confidence is low or `recognized CTN != label total`, automatically run supplemental passes:
  1. sparse-text PSM pass;
  2. centered Honeywell-screen crop + high-contrast pass.
- Supplemental results accept only lines whose principal content is one valid 7-character CTN.
- RT-description fragments are not accepted as CTNs.
- Results are merged by RT before the existing cross-page/unique-CTN reconciliation.
- UI wording changes `已辨識 xx%` to `OCR原始信心 xx%`; pass/fail is still determined by RT Master + expected count + CTN validation, not raw OCR confidence.
- Existing production-write safety guard remains enabled.

## RC bottom quick-bar persistence
`rc-quickbar-keeper-v6.js` protects two RC UI layers:
- DS Workstation bottom navigation: repairs stale iOS `ds-keyboard-open` state after keyboard close / page resume / module switching.
- Grinding `stickyActions` quick action bar: if WIP items remain selected and the HT/DCYL return sticky is not taking over, rerun formal `renderSticky()` and recover an incorrectly hidden bar.

## Rollback
1. Revert `ds-app-grinding-recovery-rc/index.html` to commit before `e2e850187a9255dd517e1e9b06153acd347d22c9`.
2. The two new RC files may remain in the repository inert, or be deleted:
   - `iqc-ocr-multipass-hook-rc-v2.js`
   - `rc-quickbar-keeper-v6.js`
3. No production backend/data rollback is required because IQC production write remains locked.

## Field acceptance targets
- Re-test the same Honeywell image where RT `112639` previously returned `12/13`; target is `13/13` without introducing a false CTN.
- RT `113374` must remain incomplete if only 16 of its 19 CTNs are actually present in uploaded photos.
- Repeatedly switch Home / Daily / Dashboard / Grinding / More and open/close keyboard; bottom DS navigation must recover and remain available after keyboard dismissal.
- In Grinding WIP, select items repeatedly before/after refresh and RC return-panel usage; the normal quick action bar must not remain accidentally hidden.
