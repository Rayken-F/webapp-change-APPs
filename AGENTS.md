# 日報系統：接手前先讀 RULER

開始處理本儲存庫的任務，包含新聊天室接手時，先完整讀取根目錄 `RULER.txt`。再讀本次直接相關的交接及實際版本，不僅依賴記憶摘要。

固定分工：助手負責版本管理、GitHub 前端修改／提交／發布及結果核對；使用者負責後端程式更新部署與驗收。只改本次必要範圍，其餘不動。

`RULER.txt` 是使用者的協作規則主檔。若無法讀取，立即說明，不得宣稱已讀取。本提醒是儲存庫文件，不代表已修改 ChatGPT 的跨聊天室記憶或保證每個新聊天室會自動載入。
